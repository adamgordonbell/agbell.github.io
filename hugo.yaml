baseURL: https://adamgordonbell.com/
languageCode: en-us
title: Adam Gordon Bell
theme: adamgordonbell

# Configure content paths
section:
  post:
    path: blog

# Set permalinks to maintain the same URL structure
permalinks:
  blog: /posts/:contentbasename.html

# Allow raw HTML in markdown
markup:
  goldmark:
    renderer:
      unsafe: true

params:
  description: "Personal blog of Adam Bell"
  sections:
    blog:
      title_short: "Blog"
      description: "Older posts and notes."

# Configure pagination
paginate: 10
paginate_path: "/page/:num/"

# Enable tags taxonomies
taxonomies:
  tag: tags

# RSS Feed
outputFormats:
  RSS:
    mediatype: "application/rss+xml"
    baseName: "rss"

menu:
  main:
    - name: Home
      url: /
      weight: 1
    - name: About
      url: /about/
      weight: 2
    - name: Speaking
      url: /speaking/
      weight: 3
    - name: Writing
      url: /writing/
      weight: 4
    - name: Podcasts
      url: /podcasts/
      weight: 5
    - name: Timeline
      url: /timeline/
      weight: 6
    - name: Blog
      url: /posts/
      weight: 7
    - name: Tags
      url: /tags/
      weight: 8
