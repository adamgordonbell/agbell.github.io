# Per-item editorial overrides for se_radio.
# Base comes from data/se_radio.json — regenerated by scripts/sync-se-radio.py.
# Set any field here and it overrides the base at render time.
# Key by slug (last path segment of the canonical URL).

