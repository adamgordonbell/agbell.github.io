# Per-item editorial overrides for pulumi_blog.
# Base comes from data/pulumi_blog.json — regenerated by scripts/sync-pulumi-blog.py.
# Set any field here and it overrides the base at render time.
# Key by slug (last path segment of the canonical URL).

