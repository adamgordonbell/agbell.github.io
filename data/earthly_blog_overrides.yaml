# Per-item editorial overrides for earthly_blog.
# Base comes from data/earthly_blog.json — regenerated by scripts/sync-earthly-blog.py.
# Set any field here and it overrides the base at render time.
# Key by slug (last path segment of the canonical URL).

