# Per-newsletter editorial overrides.
#
# Base data (title, date, url, description) comes from data/newsletters.json,
# regenerated by scripts/sync-newsletters.py. Any field set here OVERRIDES the
# base at render time.
#
# Key by slug (last path segment of the newsletter.corecursive.com URL).
#
# Example:
#
# the-2-sigma-problem:
#   description: "What Benjamin Bloom found when one-on-one tutoring beat
#     classroom learning by two standard deviations, and what AI changes."
#   notable: true

# Add entries below.
